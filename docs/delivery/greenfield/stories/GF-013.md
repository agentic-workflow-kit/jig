---
id: "GF-013"
title: "Immutable artifact storage and protected routing"
phase: 1
size: "L"
status: "proposed"
story_file: "docs/delivery/greenfield/stories/GF-013.md"
governing_paths:
  [
    "docs/redesign/design/evidence-handling.md",
    "docs/redesign/design/runtime.md",
    "docs/redesign/design/mechanism-and-provider-contracts.md",
  ]
stable_ids:
  [
    "RT-EVIDENCE",
    "PORT-ARTIFACT",
    "CB-STORE",
    "EV-ARTIFACT-FACT",
    "DR-6",
    "OPC-ART-PUT",
    "OPC-ART-GET",
    "OPC-ART-DISPOSE",
    "RT-WITNESS",
    "FC-TRUST",
    "CF-TRUST-STOP",
    "CF-EVIDENCE-LIFECYCLE",
    "CF-MECH-ARTIFACT",
    "CF-MECH-LEDGER",
  ]
product_routes: ["PC-USE-6"]
imported_commitments: []
dependencies: ["GF-002", "GF-005", "GF-010"]
dependency_edges:
  - from: "GF-002"
    type: "implementation"
  - from: "GF-005"
    type: "implementation"
  - from: "GF-010"
    type: "implementation"
dr_gates: ["DR-6"]
outcome: "Five protected and six disposable artifact classes with two-pin lookup and independent witness."
oracle:
  ["CF-MECH-ARTIFACT", "CF-MECH-LEDGER", "protected/disposable guard corpus"]
---

# GF-013 — Immutable artifact storage and protected routing

## Outcome, value, and why now

Deliver immutable artifact storage with deterministic holder-class routing: five pre-Run configuration/intake classes are protected, and six controller-mediated evidence classes are disposable only through an exact two-pin, independently witnessed lookup. It enables later evidence binding while ensuring retention/disposal cannot counterfeit authority. Acceptance, evidence sufficiency, exports, intake behavior, provider qualification, and direct storage access remain unavailable.

## Governing paths and stable IDs

`PC-USE-6` routes the protected evidence guarantee. `evidence-handling.md` owns classes/routing/immutable evidence; `runtime.md` owns `RT-EVIDENCE`, `PORT-ARTIFACT`, modes and `EV-ARTIFACT-FACT`; `mechanism-and-provider-contracts.md` owns `CB-STORE`; and `RT-WITNESS` carries independent disposable-index currency. Here `CB-STORE` binds one exact artifact Operation to its scripted, unapproved fixture/resource scope, existing protected-or-disposable holder context, artifact subject/digest, and exact mode: `OPC-ART-PUT` alone puts/verifies bytes plus the exact temporary-event/intended-holder two-entry set; `OPC-ART-GET` is read-only; `OPC-ART-DISPOSE` is only `release-pin` (no bytes) or guarded owner-authorized `dispose-bytes`. `CP-MEDIATOR` validates that complete binding and `CP-TRANSITION` alone adopts or retires its fact and advances the witnessed lookup head. The binding cannot cross artifact/resource scope, substitute a holder class, alias protected and disposable contexts, turn release into deletion, or access `PORT-LEDGER` state directly. `FC-TRUST` is the trust-root failure, `CF-TRUST-STOP` proves its stop posture, `CF-EVIDENCE-LIFECYCLE` proves immutable artifact handling, and `CF-MECH-ARTIFACT`/`CF-MECH-LEDGER` exercise the scripted semantic contracts. `DR-6` delegates only physical storage technique.

## Dependencies plus start evidence

Require merged GF-002 canonical artifact identity/digest codec, GF-005 operation intent/authority kernel, and GF-010 exact ledger/witness pass. Verify the exact external activation and separately bind approved package `P = Q + durable R identifier + PASS`, any authoritative landing-equivalence record for `P`/`Q`, and the observed execution-base ref/commit/tree with predecessor containment. Also verify the deterministic holder-class matrix, both non-aliasing contexts, independent disposable-pin witness line, and an explicit scripted artifact fixture binding marked unapproved and unreachable from configuration/dispatch. GF-022's approved provider manifest is a later gate and is not start evidence for this semantic story. A holder scan, path label, same-object copy, or unreviewed adapter cannot prove start safety.

## In-scope and non-goals

In scope: immutable put/get, routing guard, protected continuity, disposable reverse-reference pin lookup, two-pin registration, release receipt, guarded deletion preconditions, and ledger-owned witness acknowledgement. Exclude evidence manifest semantics (GF-014), acceptance/export/retention policy, intake orchestration, provider approval, cleanup lifecycle, and direct consumer/provider storage access.

## Runtime/product unit, ports, inputs, outputs, identities, persisted facts, authority

`RT-EVIDENCE` is passive and exposes only `PORT-ARTIFACT` through `CP-MEDIATOR`; `CP-EVIDENCE` requests operations, `CP-TRANSITION` adopts/retires facts and advances `LG-WITNESS` via `PORT-LEDGER`, while `RT-WITNESS` holds the one deployment-scoped disposable-index head. Inputs bind the complete `CB-STORE`: explicit scripted, unapproved, unreachable fixture binding and resource scope (the approved `ID-MANIFEST` follows only at GF-022/GF-026), holder class, exact artifact digest/subject, operation/fence, temporary event and intended durable-holder tuples. Persist immutable object bytes/digest, immutable protected continuity, disposable hash-chained lookup mutations, returned `(position, headDigest)`, and witness line. The store can never directly move/dispose/alias protected addresses, cross-bind another subject/resource/holder context, write witness, adopt facts, or decide lifecycle; `CP-EVIDENCE` cannot direct-mutate, and a witness gives no authority.

## Observable success and terminal outcomes

Success is immutable digest-verified retrieval and, for disposable material, an `EV-ARTIFACT-FACT` proving a flushed exact two-pin mutation followed by matching witness advance before acknowledgement. Adoption changes no lookup; rejection retires both tuples before release. Protected disposal/move/alias is rejected. This story cannot accept/reject a Candidate, land, release dependencies, or autonomously dispose bytes. An uncertain mutation retains/preserves pins; trust failure stops rather than silently deletes.

## Failure, uncertainty, timeout, retry, reconciliation, recovery, cleanup, and resume

Malformed/oversize/noncanonical subject/digest/context is `FC-INPUT`/`FC-SUBJECT`; stale operation/fence is `FC-FENCE`; missing content, pin tuple, mode/owner authorization, or prohibited protected operation fails closed as `FC-EVIDENCE`/`FC-AUTHORITY`; changed resource binding, fork/rollback/behind/mismatched/missing witness or unverified continuity is `FC-TRUST`. `BND-WAIT-MECHANISM` bounds mediated requests and `BND-RETRY` limits attempts; `BND-WAIT-LEDGER` covers witness commit and exhaustion enters Recovery. Before effect, intent is GF-005/015-recorded; an uncertain put/release is reconciled by stable `ID-OP`, exact lookup head and witness, never retried blindly. Confirmed absence plus recorded reauthorization may retry same effect; otherwise preserve pins and park/escalate. On restart `CP-RECOVERY` verifies class-to-context binding and lookup chain/head against witness before adoption, release, or dispose. Cleanup is only future lifecycle-authorized release/guarded delete; preserve protected objects and uncertain disposable references.

## Security and trust: validation, credentials, redaction, and authority widening

Bound byte size/type/digest and canonical metadata before storage; immutable content addressing never permits overwrite. The scripted fixture has no credential, provider, or configurable manifest path; later selected-provider credential names are manifest-declared and resolve only in memory. Objects/evidence/logs never contain secrets, and errors redact locators/credentials. Trust roots are the explicit fixture binding plus an independent witness that does not share RT-EVIDENCE/pin lookup/backups; the approved exact artifact manifest/resource binding is introduced only by GF-022/GF-026. Both routes are fixed by existing holder class, never user/provider choice; protected/disposable copies of equal bytes are separate objects. Selected artifact providers remain unreachable until GF-026 records their exact provider gate; the two CF suites here exercise scripted semantics only.

## Deliverables without delegated algorithm invention

Deliver artifact semantic contract/routing guards, scripted protected/disposable and crash fixtures, and exact suite evidence at responsibility seams; GF-001 decides package layout. **DR-6:** owner Engineering; five/six routing, non-aliasing, exact two-pin lookup, witness order, and restore currency are immutable constraints. Evidence is fixture/source identity and suite result; fallback is preservation/provider unreachable or trust stop. Any constraint widening requires an owner decision; the routing contract is design-fixed. GF-026 owns real file artifact qualification.

## Test and evidence obligations

Test each eleven holder class route; protected move/dispose/alias rejection; immutable put/get digest/read vectors; exact and partial/digest-only two-pin rejection; adoption/rejection retirement ordering; release-pin no-delete and guarded delete authorization; flush/witness/ack crash points; lost ack/confirmed absence/uncertain mutation; restore behind/fork/mismatch/missing witness; stale fence/context/manifest; hostile oversized/secret-like input. `CF-MECH-ARTIFACT` oracle checks all port/mode guards and two-pin rules; joint `CF-MECH-LEDGER` checks witness ordering/currency. Record exact artifact/object/holder/lookup/witness identities, build/manifest/environment, fixture and probe versions, fault point, redacted result. Acceptance/E2E are omitted: no acceptance/delivery path exists.

## Exact acceptance

Independent reviewer receives one candidate digest, class routing table, immutable and pin contracts, exact artifact/ledger suite evidence, witness/manifest bindings, redaction scan, and CI/check metadata. Incomplete holder coverage, self-attestation, stale/different context/witness evidence, or unresolved findings fails closed. Candidate approval is not storage deployment or landing; authoritative landing is a separate merged-tree CI proof.

## DR choices

Only DR-6 applies, with selection/owner/constraints/evidence/fallback stated above. Algorithms may not infer a pin set from visible holders; no other authority/policy choice is delegated.

## PR boundary, relative size, and split

**L.** The semantic oracle is non-aliasing protected routing plus witnessed disposable two-pin currency. Expected paths are responsibility seams, scripted fixtures, and evidence; GF-001 decides layout. The semantic half remains green and selected providers unreachable until GF-026.

## Definition of Ready

- GF-002/GF-005/GF-010 exact merged evidence is contained in the observed execution base; approved `P` and any landing-equivalence record remain separate; the explicit scripted, unapproved artifact fixture binding/resource scope, two contexts, and independent witness line are current.
- Holder classes, operation modes, ports, writers, facts, bounds/failures/recovery, and suite oracles are explicit.
- DR-6 and split/fallback are recorded; no artifact adapter is configured.

## Definition of Done

- Protected no-move/no-dispose/no-alias and disposable exact-two-pin flush-witness-ack rules are implemented and independently evidenced.
- All uncertainty reconciles or preserves; no lookup is inferred, no direct mutation/witness write is possible, and recovery verifies currency before action.
- Exact suites, secret/redaction negative tests, `pnpm check`, `git diff --check`, CI, and independent review pass.
