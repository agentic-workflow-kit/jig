---
id: "GF-043"
title: "Serialized finalizer and target authority"
phase: 4
dependencies: ["GF-012", "GF-015", "GF-031", "GF-032", "GF-040", "GF-042"]
dependency_edges:
  [
    { "from": "GF-012", "type": "implementation" },
    { "from": "GF-015", "type": "implementation" },
    { "from": "GF-031", "type": "implementation" },
    { "from": "GF-032", "type": "implementation" },
    { "from": "GF-040", "type": "implementation" },
    { "from": "GF-042", "type": "implementation" },
  ]
size: "L"
governing_paths:
  [
    "docs/redesign/design/concurrency-and-finalization.md",
    "docs/redesign/design/forge-and-landing.md",
    "docs/redesign/design/lifecycle-catalogs.md",
    "docs/redesign/design/data-and-identity.md",
  ]
stable_ids:
  [
    "CP-FINALIZER",
    "ID-AUTH",
    "ID-TARGET",
    "BND-REFRESH",
    "RC-FINALIZER",
    "RT-CONTROLLER",
    "RT-REGISTRY",
    "PORT-DELIVERY",
    "EV-TARGET-FACT",
    "EV-WAKE-AUTHORITY",
    "EV-WAKE-FINALIZATION",
    "OPC-DEL-ANCHOR",
    "FC-AUTHORITY",
    "FC-BOUND",
    "BND-WAIT-CAPACITY",
    "BND-WAIT-TARGET",
    "CF-ORDER",
    "SCH-CANDIDATE",
    "ID-CAND",
    "EV-WORKSPACE-FACT",
    "refresh",
    "target_stability",
    "finalizer_queue",
    "CF-FENCE",
    "CF-CAPACITY",
  ]
dr_gates: ["DR-3", "DR-9"]
story_file: "docs/delivery/greenfield/stories/GF-043.md"
outcome: "Comparator-least serialized finalization, target anchor, refresh/rebind, and mandatory re-review on change."
oracle: ["CF-ORDER", "CF-FENCE", "CF-CAPACITY"]
status: "proposed"
baseline_commit: "b860891d9102e0bdda1d23def81b1b974a4a26ac"
product_routes: []
imported_commitments: ["MERGE-1", "MERGE-2", "MERGE-3", "MERGE-4", "MERGE-5"]
---

# GF-043 — Serialized finalizer and target authority

## Outcome, value, and why now

Give exactly one comparator-least Accepted Story per target durable finalization grant, target-basis/anchor authority, finite refresh, and mandatory re-review after Candidate change. It needs registry, recovery, capacity/bounds, acceptance, verify semantics; it never publishes/merges/lands/releases, which GF-044 owns.

## Governing paths and stable IDs

Concurrency/finalization owns one target authority, comparator, release/recovery, and no release except confirmed landing. Forge/landing owns lineage anchor and no effect before anchor. Listed IDs map finalizer, registry/target/grant, capacity/bounds/wakes, target facts, and CF-ORDER/CF-FENCE/CF-CAPACITY.

`CP-FINALIZER` inside `RT-CONTROLLER` uses `RT-REGISTRY` to bind `ID-AUTH` to `ID-TARGET` and
`RC-FINALIZER`; `SCH-CANDIDATE`/`ID-CAND`, `EV-WORKSPACE-FACT`, and `EV-TARGET-FACT` establish the
exact Candidate/basis. `OPC-DEL-ANCHOR` is the fenced anchor operation; `FC-AUTHORITY` and
`FC-BOUND` are its authority/bound failures; `BND-REFRESH`, `BND-WAIT-CAPACITY`, and
`BND-WAIT-TARGET` are finite parked waits. `EV-WAKE-AUTHORITY`, `EV-WAKE-FINALIZATION`, `refresh`,
`target_stability`, and `finalizer_queue` are wake/progress facts only. `CF-ORDER`, `CF-FENCE`, and
`CF-CAPACITY` prove ordering, fencing, and reservation. No product route is carried. `MERGE-1`
requires independent policy-aligned evidence; `MERGE-2` reserves push, PR creation, and merge to
the runner; `MERGE-3` makes done conditions policy-bound; `MERGE-4` separates done from merged;
and `MERGE-5` requires safe Blocked-work surfacing.

## Dependencies plus start evidence

Require merged current GF-012/015/031/032/040/042 and freeze target/registry realization, comparator priority-ordinal-Story ID, capacity reservation, accepted package/Candidate/basis, policy, rule surface, target observation. Changed package/basis/policy/rule/registry invalidates start; no real delivery provider/merge exists.

## In-scope and non-goals

Scope is queue/admit, grant/rebind/release, target/anchor observation, fence, refresh and Candidate-changing re-review, recovery. Exclude publication, acceptance, checks, request/publish/merge/status/comment, content comparison/landing/release, provider configuration, retirement. Anchor is scripted authority contract only; real effect is GF-044/GF-061.

## Runtime/product unit, ports, inputs, outputs, identities, persisted facts, authority

RT-REGISTRY is witnessed grant source; controller/CP-FINALIZER alone proposes target intents; scheduler supplies RC-FINALIZER; PORT-DELIVERY returns scripted facts. Inputs bind Accepted package, target/registry, comparator/reservation, Candidate/basis, policy, ID-AUTH, generation/fence, anchor. Ledger/registry write queue/grant/rebind/release/refresh/wake/invalidation before dispatch. Only scoped current holder acts; no worker/reviewer/provider/projection/old holder crosses Story/target/basis/registry/generation fence.

## Observable success and terminal outcomes

Success is one witnessed current grant for comparator-least eligible Story matching lineage and exact basis; others wait without power. A Candidate-changing refresh retains the Story's ownership and deterministic wait position, mints a new `ID-CAND`, requires renewed full GF-040 review, then atomically rebinds the same finalization authority with no unowned interval or requeue. Ordinary implementation rework alone releases finalization authority. Refresh, capacity, and target wait exhaustion park; effect uncertainty reconciles and indeterminate facts park. Only exact Story-bound owner reject-story yields Rejected, only direct Blocked/Rejected dependency yields Pending NotRun, and missing start/provider facts do neither. Run Stopped is overlay-only. No Land/dependency release is possible here; only confirmed GF-044 landing releases dependents.

## Failure, uncertainty, timeout, retry, reconciliation, recovery, cleanup, and resume

Wrong/stale grant/anchor/cross target is FC-AUTHORITY, FC-FENCE, or FC-SUBJECT; bad fact FC-EVIDENCE; capacity FC-CAPACITY; expiry FC-BOUND. Record grant/intent first. Reobserve uncertain anchor: same registry succeeds, other parks, only confirmed absence plus reauthorization retries. Recovery rebuilds registry then authority/anchor/target before resume. A refresh retains ownership/wait position until atomic rebind; no release is dependency release, which only landing causes. GF-046 cleans later.

## Security and trust: validation, credentials, redaction, and authority widening

Validate IDs/digests/order/capacity/generation/anchor against witness; unknown/stale/cross-scope fails. Target/provider fact is evidence not grant. Credentials are named provider-memory refs; redact correlation/target metadata and quarantine hostile input. Fixture is non-configurable; no adapter gains observe/authority before qualification.

## Deliverables without delegated algorithm invention

Deliver grant/queue/fence state, target/anchor schemas, refresh/rebind/re-review transitions, fixture, recovery matrix, CF catalog. DR-3 is immutable comparator/grant tables with permutation proof/reject gaps. DR-9 is validated capacity ranges/boundary proof/preflight reject. Different ordering/authority/refresh needs owner decision.

## Test and evidence obligations

Unit/schema grant/queue/anchor/refresh/release; contract registry/target scope; negative authority stale/duplicate/cross/reviewer/provider; adversarial ties/races/policy-rule changes/hostile facts/starvation; crash/recovery/timeout every boundary. Scripted E2E proves order/refresh/re-review but no landing. Record CER/build/registry/manifest/environment and run three CF suites.

## Exact acceptance

Independent review freezes Candidate plus comparator/capacity, witness/grant, ID-AUTH fence, Accepted/verify binding, anchor facts, refresh/review trace, failures/bounds/CER. PASS proves serialization only, not target permission/provider/landing; GF-044 needs authoritative content proof.

## DR choices

DR-3 Engineering immutable comparator/grant/rebind tables, fixed total order/replay oracle, permutation proof, reject gaps. DR-9 Configuration/policy declared ranges/boundary proof, preflight reject. No DR-4/DR-5 provider choice.

## PR boundary, relative size, and split

L; one target-grant/refresh/re-review authority oracle. Include registry/controller/finalizer state, observation fixture, scheduler hooks, tests. Exclude real anchor/merge/retirement. GF-043a queue/grant may precede anchor schema only disabled; never split grant from fence.

## Definition of Ready

- All named predecessor commits are merged on the selected baseline with current exact subject evidence.
- Governing IDs, port, principal, fence, durable facts, typed failures, finite bounds, recovery route, oracle, DR record, and PR boundary have independent readiness review.
- Unqualified providers/effect paths are unreachable and configuration cannot widen authority.

## Definition of Done

- Versioned immutable inputs validate and stale, malformed, ambiguous, oversized, cross-scope, or self-attested facts fail closed.
- Intent precedes every effect; uncertain effect is reconciled by stable identity and only confirmed absence plus recorded reauthorization permits same-effect retry.
- Required unit, contract, negative-authority, adversarial, replay, crash, timeout/recovery, provider-or-scripted, and E2E evidence is exact-subject-bound; git diff --check, repository checks, CI, and independent exact-candidate review pass.
