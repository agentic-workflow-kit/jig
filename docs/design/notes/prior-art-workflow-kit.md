---
title: M5a prior art & reuse log — workflow-kit v-next
status: draft — reuse log (reference-only)
methodology: ddd
---

# Prior Art & Reuse Log — workflow-kit v-next

The retiring `workflow-kit` prototype (local at `~/repos/workflow-kit`, branch `v-next`) is
**reference-only** prior art. Per org rule, we harvest lessons and reusable engineering but never
port its architecture, and we re-derive every jig artifact from jig's own product commitments.

This log makes the reference-only boundary auditable: every lesson the M5a design carries is
listed here with its prototype source and why it survives re-derivation. The author design doc
cites these inline where they apply; this file is the index of record. Paths below are from the
three reuse surveys; the author stage re-reads a specific source before it actually carries that
pattern.

## Key finding — the dry-run is greenfield

The prototype does **not** separate a dry-run from a real execution run. Its one `previewRun`
command (`packages/sdk/.../edge/operator-command`) is a shallow Work-Source candidate-count
lookup: it never enters the run-lifecycle state machine and never touches workspace, worker, or
forge machinery. There is **no reusable dry-run execution pipeline** to harvest. jig's
dry-run-first vertical (Plan Intake -> Runner -> Fence -> Records, side-effect-free) is genuinely
new work — the prototype informs the _shapes around it_, not the pipeline itself.

## Carried lessons

| #   | Lesson (carry as a pattern, not a copy)                                                                                                                                                                                                                                                                            | workflow-kit source                                                                                                         | jig home / commitment                                          | Survives re-derivation because                                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A **closed, test-enforced transition table**: illegal run/story transitions are a compile/test-time fact, not a runtime "shouldn't happen"; recovery edges require explicit authority + cited evidence.                                                                                                            | `sdk` run-lifecycle (`lifecycle-reducer`, `transition-table`, `transition-validator`); `projections-lifecycle-and-tests.md` | Run Orchestration FSM                                          | It is a property of any robust FSM. jig applies it to its own **two-level run/story** split — the prototype's single fused run FSM is _not_ carried.                                                                                               |
| 2   | **Event-sourced records**: append-only log; state/summary/metrics are pure projections replayed from it, never authored; a single leased writer with monotonic epoch fencing removes concurrent-recovery write races.                                                                                              | `sdk` `run-event-log`/`append-writer`; `event-log-and-state.md`; AD-6                                                       | Run Records & Observability; SEE-3, RESUME basis               | Directly serves "records ARE the evidence." jig re-derives its record shape from `observability-records-v0`, not the prototype's envelope.                                                                                                         |
| 3   | **Observation honesty tri-state**: never coerce unknown to zero/false; carry `available/partial/unavailable`.                                                                                                                                                                                                      | `sdk` observability records                                                                                                 | Run Records; SEE                                               | A record-quality discipline independent of architecture.                                                                                                                                                                                           |
| 4   | **Redaction as a recorded, policy-digested per-event posture** (a `redactionPolicyDigest`), not a one-off filter.                                                                                                                                                                                                  | `sdk` observability `records/*`, `artifact-ref-guard.ts`                                                                    | Run Records; SEC-1, SEE-6                                      | jig carries the discipline and defers real secret-scanning (named extension point).                                                                                                                                                                |
| 5   | **Structural authority boundary**: separate seam contracts so worker-side code literally cannot import push/merge; privileged actions go record-intent -> evidence-bound pure predicate -> runner-only execute.                                                                                                    | `sdk` `providers/{agent,forge,...}`; `runtime-flow.md`; AD-12                                                               | Runner/worker boundary; FENCE-3, SEC-3, MERGE-2                | This is jig's boundary invariant — but jig keeps the **Fence as its own bounded context** (the prototype fused it into Completion; not carried).                                                                                                   |
| 6   | **Authorization adjudicated in the control plane from the append path**; the worker/provider never gates; the request is durably appended as a **barrier before** any decision logic, so a crash mid-decision is recoverable.                                                                                      | `sdk` `decide-approval.ts`, `classify-approval-risk.ts`; `human-control-and-approvals.md`                                   | Policy & Authorization (Fence); FENCE-1 fail-closed            | Concrete fail-closed mechanic. jig chooses its own invocation shape (not necessarily an interceptor object).                                                                                                                                       |
| 7   | **Fixed-category, no-model policy** — independent convergence on CFG-10: control decisions are pure functions of recorded evidence; LLM adjudication is structurally deferred.                                                                                                                                     | `sdk` `posture-catalog.ts` (`orchestrator-decide` deferred); AD-14                                                          | Policy posture (assisted); CFG-10                              | Two independent designs landed here — validation, cited as prior-art support. The prototype's **axis** (command-allowlist + grant-scope-ladder + 3 tiers) is _not_ carried; jig keeps its binary reversibility/privilege/rule-governance boundary. |
| 8   | **CLI as a thin adapter**: one command -> one control-plane call -> one audit event, even on invalid input; the edge holds zero run logic and imports no provider contracts; run identity allocated only after the audit append succeeds.                                                                          | `command-surface-and-envelopes.md`; `cli` operator-smoke shims                                                              | Operator entry point                                           | The prototype has no real CLI to port (only the contract shape); the invariant is re-derivable.                                                                                                                                                    |
| 9   | **Closed-environment credential keeping** at the runner/host boundary: the host never resolves secrets; `environmentMode: "closed"` + a structural predicate (`party=worker & kind=forge` -> always false); worktree-per-task + commit-only-in-worktree + runner-does-push is a cheap, provable isolation pattern. | `fnd-04` credentials-and-secrets; AD-12; `.agents/skills/orchestrated-delivery`                                             | FENCE-3, SEC-3                                                 | This is the concrete recipe for the (deferred) real local worker — design-level lesson now.                                                                                                                                                        |
| 10  | **Honesty probes**: bind verification evidence to a precomputed `commandDigest`; prove the agent's spawned commands run inside the host's owned containment (`preservesHostProcessParentage`) rather than trusting self-report.                                                                                    | execution-host `contracts-and-conformance.md`; `capabilities-and-conformance.md`                                            | Execution Host / Agent seams (named extension points); DRIVE-3 | Design-level lessons for when those seams are exercised later.                                                                                                                                                                                     |

## Post-M7 disposition — 2026-07-04

M7's real-provider spine (Phases 6-9) absorbed some prior-art inputs by re-deriving them through jig's
own ADRs. This section records the absorbed/open split after the M7 closeout. It does **not** relax the
reference-only rule above: the prototype remains input evidence only, and every absorbed claim below is
cited to the jig ADR or design doc that now owns the decision.

### ABSORBED

| Prior-art input                                                                                       | Post-M7 disposition                                                                                                                                                                                                                                                                            | Citable jig authority                                                                                                                                                                                                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Real agent/host seam, structural no-landing boundary, and deny/park rather than self-widening         | Absorbed for the real-driver spine: the Codex-first agent sits behind unchanged `AgentPort`, has no landing surface, and parks/interrupts through the Fence on denied or unavailable capability.                                                                                               | [ADR 0022](../decisions/0022-phase-6-real-driver-integration.md), Decision 2, "Real agent driver (Codex-first) behind `AgentPort`, selected through the composition root"; [providers.md](../contracts/providers.md), "Phase 6 realization (ADR 0022)"                                                                                                                        |
| Proven-not-reported capability, confinement proof, self-report rejection, and adversarial conformance | Absorbed as a core-judged Phase-6 boundary: autonomy is judged on `provenIsolationStrength`, unproven or overstated isolation records a failure token, and the conformance anchor includes forbidden-method, overstated-isolation, substrate-escalation, and drifted-resume-attestation cases. | [ADR 0022](../decisions/0022-phase-6-real-driver-integration.md), Decision 4, "Proven confinement: `provenIsolationStrength` from an exercised check, and the failure-token discipline"; Decision 8, "Real freshness clock, redaction activation, and the two regression anchors"                                                                                             |
| Closed-environment credential/substrate keeping and immutable run authority                           | Absorbed at design altitude: the substrate manifest is a hashed, approved, immutable-for-the-run tuple; out-of-tuple argv, credential, egress, or runtime requests fail closed and surface as diagnosable stops.                                                                               | [ADR 0022](../decisions/0022-phase-6-real-driver-integration.md), Decision 7, "Provider manifest + load-time substrate authorization — **IN**, at design altitude, unfrozen"                                                                                                                                                                                                  |
| Real freshness and redaction as a recorded safety posture                                             | Absorbed for real-provider records: freshness is decided by a real clock on the real path, real secret scanning activates when credentials first enter play, and redaction ambiguity is a diagnosable stop.                                                                                    | [ADR 0022](../decisions/0022-phase-6-real-driver-integration.md), Decision 8, "Real freshness clock, redaction activation, and the two regression anchors"; [records.md](../core/records.md), "Notes"                                                                                                                                                                         |
| Runner-owned real landing, exact-head re-read, and no-double-effect idempotency                       | Absorbed for the real Forge path: `done -> landed` can perform real push/PR/merge, replay recognizes prior landing from records, and a changed head stops diagnosably rather than duplicate-landing or blindly no-oping.                                                                       | [ADR 0023](../decisions/0023-phase-7-real-forge-landing.md), Decision 1, "The 7a/7b split and its acceptance-criteria assignment"; Decision 3, "Real-effect idempotency (P7-AC-3): recognize the prior landing from the records"                                                                                                                                              |
| PR-side blocked-work visibility with a durable fallback                                               | Absorbed without changing `blocked`: block surfacing is a distinct runner-invoked Forge act, and when PR-side surfacing is unsafe the block is still recorded through the durable Records fallback and never dropped.                                                                          | [ADR 0023](../decisions/0023-phase-7-real-forge-landing.md), Decision 4, "PR-side block surfacing (MERGE-5, P7-AC-5): a distinct runner-invoked Forge act, not a `land()` call"; [providers.md](../contracts/providers.md), "Phase 7 realization (ADR 0023)"                                                                                                                  |
| Work-source as candidate producer, never scheduler/authority                                          | Absorbed for real work-source intake: every candidate must cross a single `PlanValidator` chokepoint that mints a runtime-verifiable validated wrapper; raw scheduling bypasses fail closed and are recorded.                                                                                  | [ADR 0024](../decisions/0024-phase-8-real-work-source.md), Decision 2, "The INV-007 crossing (P8-AC-1/P8-AC-2): a single structural intake chokepoint every candidate passes before scheduling"; [providers.md](../contracts/providers.md), "Phase 8 realization (ADR 0024)"                                                                                                  |
| Two-authority provenance and candidate origin legibility                                              | Absorbed as an additive, unfrozen record shape: work-source provenance names source system plus candidate identifier and remains legible in records without treating driver identity alone as sufficient.                                                                                      | [ADR 0024](../decisions/0024-phase-8-real-work-source.md), Decision 3, "Richer provenance (P8-AC-3): widen `CandidateWorkItem.provenance` to an origin-bearing shape; the record carries origin without a freeze"                                                                                                                                                             |
| Payload-local integrity / hash-chain pattern over durable evidence                                    | Absorbed for M7 records integrity: protected artifacts are digested, the append-only log is hash-chained, the result is HMAC-authenticated with an environment-only key, and the materialized sidecar stays outside golden record bytes.                                                       | [ADR 0025](../decisions/0025-phase-9-records-integrity.md), Decision 1, "Integrity primitive and sidecar surface — HMAC-now, keyed from the environment, materialized on the sidecar; the trust anchor is why"                                                                                                                                                                |
| Tamper-vs-changed-basis resume split and active owner re-approval                                     | Absorbed for Phase 9: broken integrity hard-refuses resume; a legitimate safety-relevant basis change blocks on fresh owner re-approval through the existing Doorbell path; no model adjudicates the boundary.                                                                                 | [ADR 0025](../decisions/0025-phase-9-records-integrity.md), Decision 2, "Active `resume-blocked-missing-approval` — the tamper-vs-changed-basis split, re-approval through the existing owner path, CFG-10 no model adjudicates"; Decision 3, "Diagnosable stops (P9-AC-3) — named reasons at inspect and resume refusal, never silent; records stay safe to keep and export" |
| Driver binding across resume                                                                          | Absorbed as additive launch evidence: resolved launch driver selection is persisted under `run.started.binding.drivers`, digest-covered by the integrity sidecar, and verified on resume without rebinding.                                                                                    | [ADR 0025](../decisions/0025-phase-9-records-integrity.md), Decision 4, "Fold in the deferred resume driver-binding residual (T6/T8) — additive launch-header binding, verification-only on resume"                                                                                                                                                                           |

### STILL OPEN (routed)

These are still input material, not design decisions. Their routing follows the org M7 entry's
"Post-spine decisions (2026-07-04)" in `.github/MILESTONES.md`.

| Prior-art input                                                                                                                                                                  | Route                                                                                              | Disposition                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `20-sdk-and-packaging/` corpus: dependency matrix, SDK boundary, injected factory shape, testkit split, storage-ports boundary                                                   | N3 packaging + SDK-boundary ADR, after the N2 packaging product record                             | Use the form and boundary questions as input only. N3 must follow the product decision that packages stay private for now with an internal SDK boundary, and it must not assume a third-party provider ecosystem; that product sub-question remains open. |
| Codex transport research plus `codex-plugin-cc` lifecycle evidence                                                                                                               | N1: N1a fresh dated/hashed evidence capture against current Codex, then N1b transport ADR          | Prior-generation transport evidence was schema-only against Codex 0.141.0 and proves nothing about current behavior. Current `codex-plugin-cc` material is adjacent lifecycle evidence to test, not a jig transport decision.                             |
| Planning/authoring discipline standard: six story-contract rules, phantom-consumer checks, coverage dispositions, sizing, characterization review, and lessons-ledger discipline | N4 planning-discipline uplift, an org milestone candidate (M8) owned primarily by `design-to-plan` | Not a jig implementation cycle. jig can later consume validated plan-quality fields or `PlanValidator` checks, but the discipline standard belongs at the org/planning layer first.                                                                       |
| Attestation field menu and richer provider-port encodings                                                                                                                        | T14 v0 contract freeze                                                                             | Candidate encodings only. Field names, enums, proof fields, and related v0 shapes are chosen at freeze time, not by this ledger and not during N3 packaging.                                                                                              |
| `self-report-only` failure token and mock-can-lie conformance vocabulary                                                                                                         | Post-M7 design-flow item authorized in M7 wave 1                                                   | Open as vocabulary sharpening over the already-absorbed proof/conformance posture. It should be re-derived into jig's conformance and records language before any implementation claim.                                                                   |
| Recovery-state taxonomy, `safe-empty-restartable`, and 3-way action-safety classes                                                                                               | Parked until real operational incidents                                                            | Phase 9 absorbed the immediate tamper-vs-changed-basis split. The broader recovery taxonomy waits for evidence from real incidents rather than being scheduled from prototype theory.                                                                     |
| Protected-policy digest binding and head-SHA-bound approval                                                                                                                      | Parked pending overlap check against jig's plan-intake / authorization split                       | jig already owns the rule-governing-surface boundary. The prototype binding is a concrete candidate only after design checks whether it duplicates or cuts across existing plan-intake and authorization responsibilities.                                |

## Do not inherit — re-decide from jig's own constraints

- **AD-2** (TS/Node core + native containment helper) — jig picks its runtime stack from its own
  constraints, not because the prototype did.
- **AD-10** (day-one four providers, each with mock + conformance suite) — appropriate for a
  platform aiming at multi-provider day one; jig's M5a is a single local fixture. Committing to
  that seam/conformance machinery now would expand scope before the local path proves out (the M5
  "minimal expands into multi-driver portability" kill-assumption).
- **The 3-tier allowlist / grant-scope-ladder risk classifier** — heavier and differently-axised
  than jig's binary CFG-10; porting it would silently re-architect jig's category model.
- **The single fused run FSM** and **the 8-kind agent event taxonomy** — jig re-derives a coarser,
  two-level (run/story) model and only the event families its dry-run actually emits.
- **The experimental Codex app-server agent driver** — recommended on paper in the prototype but
  unproven (live conformance probes not run); not "proven machinery" to import.

## How this informs Q1 (worker posture)

The cost survey found a _thin real local worker_ (worktree workspace, closed-env subprocess,
process-group containment, runner-owned checks, diff as output) is light and buildable — its heavy
parts (live capability probes, kernel-level containment, egress negative-probes, persistent
approval-channel resume) are gated behind **autonomy**, not behind evidence-producing execution.
That makes the real local worker a **clean, bounded follow-on** with a known recipe (lessons 9-10),
which is exactly why M5b can safely defer it: see the frame's Q1 resolution.
