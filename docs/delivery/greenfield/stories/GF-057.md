---
id: "GF-057"
title: "Qualified GitHub review-publication provider"
phase: 6
dependencies: ["GF-022", "GF-041", "GF-045", "GF-046"]
dependency_edges:
  [
    { "from": "GF-022", "type": "evidence" },
    { "from": "GF-041", "type": "implementation" },
    { "from": "GF-045", "type": "implementation" },
    { "from": "GF-046", "type": "implementation" },
  ]
size: "L"
governing_paths:
  [
    "docs/redesign/design/decisions/D15-pre-acceptance-review-publication.md",
    "docs/redesign/design/mechanism-and-provider-contracts.md",
    "docs/redesign/design/review-and-verification-execution.md",
    "docs/redesign/design/forge-and-landing.md",
    "docs/redesign/design/architecture-conformance.md",
    "docs/redesign/design/product-guarantee-reconciliation.md",
    "docs/redesign/design/lifecycle-catalogs.md",
    "docs/redesign/design/delegation-register.md",
  ]
stable_ids:
  [
    "PORT-DELIVERY",
    "CB-REVIEW-PUBLICATION",
    "DR-4",
    "DR-5",
    "FC-MECHANISM",
    "BND-WAIT-MECHANISM",
    "CF-SECRET-ABSENCE",
    "CF-REVIEW-PUBLICATION",
    "CF-BLOCK-SURFACING",
    "CF-GATE-PROVIDER",
    "EV-TARGET-FACT",
    "OPC-REV-PUBLISH",
    "OPC-REV-REQUEST",
    "OPC-REV-STATUS",
    "OPC-REV-COMMENT",
    "OPC-REV-RETIRE-REF",
    "OPC-REV-RETIRE-REQUEST",
    "OPC-REV-RETIRE-STATUS",
    "OPC-REV-RETIRE-COMMENT",
    "ID-OP",
  ]
dr_gates: ["DR-4", "DR-5"]
story_file: "docs/delivery/greenfield/stories/GF-057.md"
outcome: "Qualified GitHub review-publication provider, limited to draft non-mergeable review operations and their retirement."
oracle: ["CF-REVIEW-PUBLICATION", "CF-BLOCK-SURFACING", "CF-GATE-PROVIDER"]
status: "proposed"
baseline_commit: "b860891d9102e0bdda1d23def81b1b974a4a26ac"
product_routes:
  [
    "PC-JIG-11",
    "PC-JIG-15",
    "PC-JIG-21",
    "PC-CONCEPTS-5",
    "PC-CONCEPTS-6",
    "PC-USE-5",
  ]
imported_commitments: []
---

# GF-057 — Qualified GitHub review-publication provider

## Outcome, value, and why now

Qualify GitHub only for D15's pre-acceptance review-publication capability: exact Candidate review
ref publication, draft non-mergeable request, stable status/comment, bounded Blocked surfacing, and
the catalogued review-venue retirement operations. The qualified provider remains incapable of
target change, finalization, merge, landing, or `ID-AUTH` use. GF-061 is a distinct later final-
delivery qualification subject; neither credential nor its evidence can substitute for the other.

## Governing paths and stable IDs

D15 and forge-and-landing define the review-only `OPC-REV-PUBLISH`, `OPC-REV-REQUEST`,
`OPC-REV-STATUS`, `OPC-REV-COMMENT`, `OPC-REV-RETIRE-REF`, `OPC-REV-RETIRE-REQUEST`,
`OPC-REV-RETIRE-STATUS`, and `OPC-REV-RETIRE-COMMENT` set, `CB-REVIEW-PUBLICATION`, exact
Candidate/target-basis/generation/ref/marker binding, and the draft/non-mergeable/no-target-change
limits. The mechanism contract owns real-provider admission and `BND-WAIT-MECHANISM`. `CF-REVIEW-PUBLICATION` proves the
review scope, `CF-BLOCK-SURFACING` proves its bounded Blocked marker posture, and
`CF-GATE-PROVIDER` proves the qualified profile is the only reachable one. `ID-OP` and
`EV-TARGET-FACT` identify and persist reconciled review effects; they never confer final target
authority. `DR-4` and `DR-5` delegate typed-edge/configuration realization only within D15.

Track mapping: `PC-JIG-11`, `PC-JIG-15`, `PC-JIG-21`, `PC-CONCEPTS-5`, `PC-CONCEPTS-6`, and
`PC-USE-5` close only through this review-only provider evidence. This story has no direct
imported-commitment assignment: the upstream semantic stories retain every imported-commitment
mapping, and none grants final-delivery authority here.

## Dependencies plus start evidence

Require GF-022's merged provider-authority evidence, GF-041's scripted D15 semantic contract,
GF-045's scripted Blocked-surfacing contract, and GF-046's preservation-safe review-retirement
contract. Require the exact external activation record and dynamically observed execution
base/package PASS tuple, including predecessor containment, before qualification begins. Freeze the
GitHub API/build, exact provider manifest,
review credential reference and least scope, repository/target basis, review ref/request/marker
format, selected host/environment, and all predecessor suite digests. Missing, stale, substituted,
over-broad, or mode-ambiguous evidence leaves the review-publication provider unreachable.

## In-scope and non-goals

In scope: real GitHub qualification for only `OPC-REV-PUBLISH`, `OPC-REV-REQUEST`,
`OPC-REV-STATUS`, `OPC-REV-COMMENT`, and the four `OPC-REV-RETIRE-*` operations, including their
exact effect reconciliation and Blocked marker behavior. Exclude every `OPC-DEL-*` operation,
target lineage anchor/ref mutation, target authority, `ID-AUTH`, finalizer proposal, merge,
landing, generic automation, worker/reviewer credentials, queue/held mode support, and public API.

## Runtime/product unit, ports, inputs, outputs, identities, persisted facts, authority

`CP-TRANSITION` is the sole authorizer: after it records the D15 Transition, `CP-MEDIATOR` narrows
the admitted manifest to `CB-REVIEW-PUBLICATION`, dispatches one `PORT-DELIVERY` operation, and
validates the result. Every operation binds Candidate content digest, target basis, controller
generation, dedicated review ref, draft request identity/markers, provider manifest, and stable
`ID-OP`; it deliberately omits `ID-AUTH`. Durable facts are operation intent, effect identity,
markers, observed ref/request state, receipt/failure, delivery metadata, and retirement obligation.
The provider may attest those facts only. It cannot acquire or retain finalization authority, touch
the target lineage anchor, mutate the target ref, request merge, declare acceptance/landing, or
release a dependency.

## Observable success and terminal outcomes

Success is an exact-Candidate dedicated review ref plus an attested draft, non-mergeable request
with one stable marker context; terminal Story outcomes or the terminal-stop Settlement overlay may
authorize only the four catalogued review-retirement classes. `Blocked` surfacing leaves the exact
business outcome and dependency graph unchanged. Publication, status, comment, and retirement can
never change target state or make a Candidate Accepted or Landed. A held, queue, unknown, ambiguous,
or out-of-scope observation fails closed under existing containment and leaves the provider
unavailable rather than falling through to final delivery.

## Failure, uncertainty, timeout, retry, reconciliation, recovery, cleanup, and resume

Map malformed/noncanonical Candidate/ref/marker input to `FC-INPUT`; wrong Candidate/ref/target
subject to `FC-SUBJECT`; stale generation to `FC-FENCE`; scope breach to `FC-AUTHORITY`; invalid
review fact to `FC-EVIDENCE`; API timeout/fault to `FC-MECHANISM`; and conflict/unknown effect to
`FC-EFFECT`. Record every intent first. Reconcile only by the stable marker plus exact review
ref/request observation; retry the same identity only after confirmed absence and recorded
reauthorization. Recovery rereads durable operation/fence facts, fences stale results, and preserves
draft clutter as the recorded retirement obligation. No reconnect can widen review scope or resume a
final effect.

## Security and trust: validation, credentials, redaction, and authority widening

Canonicalize repository/ref/target/marker/Candidate digest, validate draft/non-mergeable posture and
response binding, and require a review-only least-privilege credential class. Credential names
resolve only in provider memory; tokens, headers, raw URLs, and unredacted review text never enter
ledger, evidence, exports, or logs. The review credential is structurally disjoint from GF-061's
final-delivery credential. No GitHub response, draft request, review credential, or fallback can
mint `ID-AUTH`, acceptance, finalization, merge, target mutation, or landing authority.

## Deliverables without delegated algorithm invention

Deliver the private GitHub review-publication adapter, review-only profile validator, exact
ref/request/marker mapper, review-effect reconciler, review-retirement/obligation bridge, sandbox
repository fixtures, and qualification recorder. `DR-4` is Engineering plus configuration and
`DR-5` is Configuration; their work starts only under the exact external activation record and
current execution tuple, while widening remains an owner decision. D15 exclusions are fixed; the
fallback is provider unavailable. GF-061 owns the separate final-delivery provider/credential
subject and no algorithm may bridge the two capabilities.

## Test and evidence obligations

Run real sandbox-repository tests for exact Candidate/basis/generation/marker binding, draft and
non-mergeable posture, denied target/anchor/merge/`ID-AUTH` attempts, status/comment dedup, Blocked
surfaces, all retirement classes, discovery/reconciliation, timeout/API failure/unknown result,
stale generation, crash/recovery, hostile response, secret absence, and held/queue rejection.
Require `CF-REVIEW-PUBLICATION`, `CF-BLOCK-SURFACING`, `CF-SECRET-ABSENCE`, and
`CF-GATE-PROVIDER` with exact build/manifest/repository/mode/fixture/output digests. No target-
changing or landing E2E belongs to this story.

## Exact acceptance

Independent review receives exact Candidate/target/provider/review-credential/mode digests,
redacted real-provider evidence, marker reconciliation traces, and suite results. Any mergeable
draft, target effect, `ID-AUTH` appearance, credential overlap with final delivery, unreconciled
uncertain effect, or enabled held/queue mode fails. PASS is only provider admission for D15's
review-publication scope and is not acceptance, finalization, or landing proof.

## DR choices

`DR-4` implementation owner is Engineering plus configuration and `DR-5` owner is Configuration.
The exact external activation record and current execution tuple authorize the recorded profile;
any widening remains an owner decision. The fixed D15 limits, real evidence, and provider-unavailable
fallback apply. Shared credentials, worker publication, landing-capability reuse, and queue/held
support are rejected alternatives.

## PR boundary, relative size, and split

L; one D15 review-publication provider qualification and its independent review-only credential/
evidence gate. Expected paths are private provider/profile/reconciler/fixture/qualification paths.
Exclude target-changing delivery and GF-061. Do not combine this PR authority subject with GF-061:
their authorizing transitions, credentials, allowed operations, and evidence are disjoint.

## Definition of Ready

- GF-022/GF-041/GF-045/GF-046 are merged with current exact provider, review-contract, Blocked-
  surfacing, and review-retirement evidence.
- D15 operation exclusions, credential class, exact binding, bounds/reconciliation, redaction,
  retirement, DR fallbacks, and no-final-delivery boundary have independent readiness review.
- No unqualified provider/effect path is reachable and configuration cannot widen review scope.

## Definition of Done

- The real GitHub review provider admits only exact draft/non-mergeable review operations and their
  retirement; denied target/finalization/landing paths leave no widened capability.
- Required real-provider, authority, fault, timeout, recovery, retirement, secret, Blocked, and
  gate evidence passes with a review-only credential class.
- Exact independent review, `git diff --check`, repository checks, CI, and provider gate pass;
  no target-changing delivery or landing proof is claimed.
