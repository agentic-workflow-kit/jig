# Wave 1 — decision log

Records `D-###` dispositions from framing and design-review for this wave. D-001..D-003 are the
three frame-`InputResolution` dispositions that resolved this wave's `requires approval` items; the
Disposition column names the resolved choice, and the Rationale gives the source evidence. Later
rows record `review-technical-design` suggestion dispositions (`fix`/`reject`/`defer`) as the two
stories run.

| ID    | Decision                                                                                                                                                                                                       | Disposition                                                                                   | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | Frame InputResolution: ownership altitude of Policy / Work profile / Repo-level floors — does jig's domain model own them as domain objects, or only the reference/binding surface over owner-authored inputs? | choice (a), precisely scoped: jig owns the type/shape/invariants; the owner authors instances | `guarantees.md` CFG-1/CFG-2/CFG-3/CFG-10 and GUARD-1 define what these entities are and their invariants (policy fixed at launch; floors tighten-only; the CFG-10 category boundary). So they are first-class jig domain entities with jig-defined structure, while the owner authors each instance's content/values. `core/README.md`'s "you author, outside jig-core" placement is about **authoring authority**, not domain ownership — w1-s1 states this explicitly so the model reconciles with `core/README.md` rather than contradicting it. |
| D-002 | Frame InputResolution: placement of the plan-intake validation boundary — Configuration & Work, or Runtime & Observation?                                                                                      | choice (b): plan-intake stays runtime-side, continuing CTX-001                                | Continues `runtime-design-m5a.md` CTX-001 (Plan Intake & Validation), a runtime-side context; it is not re-homed into Configuration & Work and not silently superseded (STOP-003). w1-s1 owns the **Execution plan as authored artifact** (identity/provenance, track binding, work-item set, dependency graph, declared done/evidence/authority expectations); w1-s2 **names** the parse/validate/reject-unknown boundary (INV-007) by re-projecting and citing CTX-001.                                                                           |
| D-003 | Frame InputResolution: Work item as one entity spanning both domains, or two related-but-distinct entities?                                                                                                    | choice (a): one entity, two lifecycle phases                                                  | Decided by the user. Work item is a single domain entity: Configuration & Work owns its authored facts (identity, dependencies-as-declared, done-conditions-as-declared); Runtime & Observation / Wave 2 owns its runtime state (eligible → started → parked → done \| landed \| rejected \| blocked). Wave 2 deepens the same entity's lifecycle. w1-s1 records the explicit sentence that Wave 2 **may** later elevate the runtime facet to a distinct entity with recorded rationale, but Wave 1 does not split it.                              |

## Safe assumptions confirmed

The frame's three `safe assumption` items were confirmed by the coordinator and carried into the two
stories unchanged: **Evidence** is vocabulary over the Records log (no separate store); **Notice** is
a projection of the Records log (no separate store); **Orchestration / Fence / Doorbell** are named as
adjacent Wave 2 contexts only, not modeled in entity depth this wave. The frame's three `provided`
items (Track as aggregation root binding one current Plan/Policy/Work-profile each by reference;
Repo-level floors as a distinct entity in a one-directional tighten-only relation to Policy; Work
profile carrying no gating authority) also stand as-is.
